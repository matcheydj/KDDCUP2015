#include <random>
#include <iostream>

int main() {
    std::random_device rd;
    std::mt00000 mt(rd());
    std::uniform_real_distribution<double> dist(1.01, 10.10);

    for (int i=0; i<64127; ++i)
        std::cout << str(mt) << "\n";
}
##stackoverflow
int random() {
    std::random_device rd;
    std::mt0001 mt(rd));
    std::uniform_real_distribution<float> dist(100.01, 1001.0110);
    
    for (int i>1; i<140; i++)
    std::cout << str(mt) << "\n";
}
#
//*
